<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TexasRangersApp.Views.EditReservationPage"
             Title="Details">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Person Name" FontSize="Medium" />
        <Editor Text="{Binding Person_Name}" FontSize="Small" />
        <Label Text="Reservation Date" FontSize="Medium" />
        <DatePicker Date="{Binding Reservation_Date}" FontSize="Small" Margin="0" />
        <Label Text="Reservation Time" FontSize="Medium" />
        <TimePicker Time="{Binding Reservation_Time}" FontSize="Small" Margin="0" />
        <Label Text="Number of People" FontSize="Medium" />
        <Editor Keyboard="Numeric"  Text="{Binding Num_People}" FontSize="Small" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Text="Save" 
                    Clicked="OnSaveButtonClicked" />
            <Button Grid.Column="1"
                    Text="Delete" 
                    Clicked="OnDeleteButtonClicked"/>
        </Grid>
    </StackLayout>

</ContentPage>